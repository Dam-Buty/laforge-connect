/**
 * TODO : implement sending the response on WhatsApp / Twilio
 */
export async function sendResponse(phoneNumber: string, response: string) {
  console.log("Message is being sent", phoneNumber, response);
}
